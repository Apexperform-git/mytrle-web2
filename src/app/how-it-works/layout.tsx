import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "How it Works",
  description: "OmnI watches your process flow minute by minute, learns what normal looks like, and provides foresight — reducing firefighting and revealing what matters next.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
